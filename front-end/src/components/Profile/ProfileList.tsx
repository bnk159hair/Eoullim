import React from 'react';
import ProfileListItem from './ProfileListItem';

const ProfileList = () => {
    return (
        <div>
            <ProfileListItem/>
        </div>
    );
};

export default ProfileList;